/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            "i.imgur.com",
            "imgur.com",
            "img.freepik.com",
        ],
    },
}

module.exports = nextConfig
